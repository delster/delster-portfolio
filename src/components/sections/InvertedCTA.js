import React from "react"

const InvertedCTA = props => <section className="section invertedcta" style={{ color:`white`, backgroundColor: props.bg }}>{props.children}</section>

export default InvertedCTA