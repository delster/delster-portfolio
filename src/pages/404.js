import React from "react"
import Layout from "../components/layout"

const NotFoundPage = () => <Layout title="404: Not Found" subtitle="You hit a page that doesn't exist." />

export default NotFoundPage
